import React, {Component} from 'react';
import {Layout, Text} from "@ui-kitten/components";

const LikedScreen = () => {

    return (
        <Layout style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
            <Text category='h1'>LIKED SCREEN</Text>
        </Layout>
    )
}

export default LikedScreen;
